
# fullstack-b2b
Fullstack B2B application built with React (frontend) and FastAPI (backend).

# پروژه فول‌استک B2B 🚀 (React + FastAPI)

این پروژه یک اپلیکیشن فول‌استک B2B است که:
- **فرانت‌اند** با **React**
- **بک‌اند** با **FastAPI**
پیاده‌سازی شده و برای توسعه سریع و مقیاس‌پذیر آماده است.

---

## ✨ تکنولوژی‌ها
- **Frontend:** React
- **Backend:** FastAPI (Python)
- **API:** REST
- **Database:** (اینجا دیتابیس‌ت رو بنویس)

---

## 📁 ساختار پروژه
- `B2B-front/`  → کدهای فرانت‌اند (React)
- `B2B-backend/` → کدهای بک‌اند (FastAPI)

---

## ▶️ اجرا در سیستم

### اجرای بک‌اند (FastAPI)
```bash
cd B2B-backend
python -m venv venv
# ویندوز:
venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload


لینک‌ها:

API: http://127.0.0.1:8000

مستندات Swagger: http://127.0.0.1:8000/docs

اجرای فرانت‌اند (React)
cd B2B-front
npm install
npm run dev
git add README.md
git commit -m "Fix README merge markers"
git push


فرانت:

http://localhost:5173
 937f1a7 (Initial commit: FastAPI + React fullstack)
